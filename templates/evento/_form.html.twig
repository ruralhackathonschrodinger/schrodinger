{{ form_start(form) }}
	<div class="row">
		<div class="col-md-6">
			{{ form_row(form.nombre) }}
		</div>	
		<div class="col-md-6">
			{{ form_row(form.fecha) }}
		</div>
        <div class="col-md-12">
			{{ form_row(form.descripcion) }}
		</div>		
		<div class="col-md-6">
			{{ form_row(form.foto) }}
		</div>			
        <div class="col-md-6">
			{{ form_row(form.localizacion) }}
		</div>	
	</div>	
	<div class="row">
		<div class="col-md-12">
			{{ form_row(form.save) }}
		</div>
	</div>		
{{ form_end(form) }}

